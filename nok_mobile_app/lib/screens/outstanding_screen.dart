import 'package:flutter/material.dart';

class OutstandingScreen extends StatelessWidget {
  const OutstandingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Text('Hello Outstanding People.'),);
  }
}
